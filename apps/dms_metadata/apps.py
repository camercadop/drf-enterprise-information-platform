"""
App configuration for DMS Metadata.
"""

from django.apps import AppConfig


class DmsMetadataConfig(AppConfig):
    name = "apps.dms_metadata"
    label = "dms_metadata"
    verbose_name = "Document Metadata"
